public interface Activable {
    
    void Activar (contexto contexto);// Método que ejecuta la lógica de la carta

    String getDescripcion();// Método para obtener el texto de lo que hace la carta
}
